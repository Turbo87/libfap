To install libfap use the normal procedure:

./configure [--prefix=/opt or other options]
make
<become root if necessary>
make install

For detailed installation instructions see the INSTALL file.

Usage info is available at http://pakettiradio.net/libfap/docs/ or run
doxygen to generate a local copy of those pages. When in doubt, see the
fap.h header file directly.
